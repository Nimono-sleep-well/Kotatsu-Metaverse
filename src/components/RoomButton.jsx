import React from 'react'

const RoomButton = ({ room }) => {
    return (
        <div key={room.id}>
            <button>
                <img src="" alt="" />
            </button>
        </div>
    )
}

export default RoomButton